name: test

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # This workflow contains a single job called "build"
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello, world!
        
      - name: List
        run: dir
        
      - name: Download Google Drive
        run: |
          mkdir temp
          Invoke-WebRequest -Uri https://dl.google.com/drive-file-stream/GoogleDriveSetup.exe -OutFile temp/GoogleDriveSetup.exe
          ls
          ls temp
          
      - name: Download Win32 content prep tool
        run: |
          git clone https://github.com/Microsoft/Microsoft-Win32-Content-Prep-Tool
          ls
          
           
      
      

